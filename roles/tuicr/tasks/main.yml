---
- name: Tap tui-cr repository
  homebrew_tap:
    name: agavra/tap
    state: present

- name: Install tui-cr
  homebrew:
    name: agavra/tap/tuicr
    state: present

- name: Record role in lockfile
  command: "{{ playbook_dir }}/bin/lock-role {{ ansible_role_name }}"
  changed_when: false
